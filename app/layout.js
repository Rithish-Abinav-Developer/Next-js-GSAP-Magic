import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "A modern scroll-driven 3D carousel",
  description: "A modern scroll-driven 3D carousel built with Next.js and GSAP ScrollTrigger. This project showcases advanced animation techniques, React lifecycle management, responsive rendering, and optimized client-side interactions for smooth visual experiences.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        {children}
      </body>
    </html>
  );
}
